// DAGGraph — SVG DAG renderer ported from open-krode, React-ified.
// Data-driven: accepts {nodes, edges} — no hardcoded kro concepts.
// TODO: implement

export default function DAGGraph() {
  return <svg id="dag-graph" />
}
