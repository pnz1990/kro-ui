// InstanceDetail — live DAG with 5s polling, spec/conditions/events panels, node YAML.
// Stub: full implementation in spec 005-instance-detail-live.

export default function InstanceDetail() {
  return <div data-testid="instance-detail-page">InstanceDetail — coming in spec 005.</div>
}
