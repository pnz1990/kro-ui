# Tasks: 043 — Upstream Fixture Generator (Full Coverage)

**Branch**: `043-upstream-fixture-generator`
**Spec**: `.specify/specs/043-upstream-fixture-generator/spec.md`

**Format**: `[ID] [P?] [Story?] Description with file path`
- **[P]**: parallelizable (different files, no blocking dependency)
- **[Story]**: user story label (US1–US5)

---

## Phase 1: Setup

**Purpose**: Confirm baseline before any edits.

- [x] T001 Verify `test/e2e/fixtures/chain-cycle-a.yaml` exists and contains two `kind: ResourceGraphDefinition` documents (`chain-cycle-a` and `chain-cycle-b`)
- [x] T002 Confirm `chain-cycle-a.yaml` is not yet referenced in `test/e2e/setup/global-setup.ts` or `scripts/demo.sh`
- [x] T003 Confirm `github.com/kubernetes-sigs/kro` is not yet a direct dep in `go.mod`

---

## Phase 2: Foundational — Go module setup

**Purpose**: Anchor the tools-only kro dep. Blocks all Part B/C work.

**⚠️ CRITICAL**: T004–T006 must complete before any `cmd/dump-fixtures` work begins.

- [x] T004 Create `tools.go` at repo root: `//go:build tools` build tag + blank import `_ "github.com/kubernetes-sigs/kro/pkg/testutil/generator"` to anchor the dep in go.mod
- [x] T005 Run `GOPROXY=direct GONOSUMDB="*" go get github.com/kubernetes-sigs/kro@latest` to add dep to `go.mod` and `go.sum`
- [x] T006 Run `make tidy` then `go build ./cmd/kro-ui` (no `-tags tools`) — must succeed confirming main binary is unaffected

**Checkpoint**: `go build ./cmd/kro-ui` passes → Part B work can begin.

---

## Phase 3: US1 — Chain-cycle fixture parity (Priority: P1) 🎯 Shippable independently

**Goal**: `chain-cycle-a.yaml` applied in both E2E setup and demo. Journey 025 Step 4 stops silently skipping.

**Independent test**:
```bash
make demo
kubectl --kubeconfig .demo-kubeconfig.yaml get rgd | grep chain-cycle
# → chain-cycle-a and chain-cycle-b appear
```

- [x] T007 [P] [US1] Edit `test/e2e/setup/global-setup.ts`: after the chain-parent apply (line 267), add `execFile('kubectl', ['--kubeconfig', KUBECONFIG_PATH, 'apply', '-f', join(FIXTURES_DIR, 'chain-cycle-a.yaml')])` and `console.log('[setup] Chain cycle RGDs applied')`
- [x] T008 [P] [US1] Edit `scripts/demo.sh`: after the chain-parent apply line (line 188), add `info "Applying chain-cycle RGDs (cycle detection demo)…"` and `"${KC[@]}" apply -f "${FIXTURES_DIR}/chain-cycle-a.yaml" 2>/dev/null || true`

**Checkpoint**: US1 complete.

---

## Phase 4: US2 — `cmd/dump-fixtures` Go tool (Priority: P2)

**Goal**: `make dump-fixtures` writes 11 upstream fixture YAML files to `test/e2e/fixtures/`. Deterministic, idempotent.

**Independent test**:
```bash
make dump-fixtures
ls test/e2e/fixtures/upstream-*.yaml   # expect 11 files
# Each file: Apache 2.0 header + machine-generated comment present
```

**Prerequisites**: T004–T006 complete.

- [x] T009 [US2] Create `cmd/dump-fixtures/main.go`: `//go:build tools` tag, Apache 2.0 copyright header, package `main`, imports (`generator`, `krov1alpha1`, `sigs.k8s.io/yaml`, `corev1`, `metav1`, `os`, `path/filepath`, `fmt`), skeleton `main()` that calls all builders and exits non-zero on error
- [x] T010 [US2] Implement `writeFixture(path string, obj interface{}) error` in `cmd/dump-fixtures/main.go`: marshal to YAML via `sigs.k8s.io/yaml.Marshal`, prepend Apache 2.0 header lines + `# Code generated by cmd/dump-fixtures. DO NOT EDIT.` comment, write to file with `os.WriteFile`
- [x] T011 [US2] Implement `buildCartesianForEachRGD() *krov1alpha1.ResourceGraphDefinition` in `cmd/dump-fixtures/main.go`: `generator.NewResourceGraphDefinition("upstream-cartesian-foreach", generator.WithSchema("CartesianApp", "v1alpha1", map[string]interface{}{"name":"string","regions":"[]string","tiers":"[]string"}, nil), generator.WithResourceCollection("appConfigs", configMapTemplate, []krov1alpha1.ForEachDimension{{"region":"${schema.spec.regions}"},{"tier":"${schema.spec.tiers}"}}, nil, nil))`
- [x] T012 [US2] Implement `buildCartesianForEachInstance() *unstructured.Unstructured` in `cmd/dump-fixtures/main.go`: kind `CartesianApp`, apiVersion `e2e.kro-ui.dev/v1alpha1`, namespace `kro-ui-e2e`, spec `{name: upstream-cartesian-foreach, regions: [us-east-1, eu-west-1], tiers: [web, api]}`
- [x] T013 [US2] Implement `buildCollectionChainRGD()` in `cmd/dump-fixtures/main.go`: `WithResource("baseConfig", configMapTemplate with `data.enabled: "true"`, nil, nil)` + `WithResourceCollection("chainedConfigs", configMapTemplate, []ForEachDimension{{"val": "${has(baseConfig.data.enabled) ? schema.spec.values : []}"}}, nil, nil)` — schema spec has `name: string`, `values: []string`
- [x] T014 [US2] Implement `buildCollectionChainInstance()` in `cmd/dump-fixtures/main.go`: kind `CollectionChain`, namespace `kro-ui-e2e`, spec `{name: upstream-collection-chain, values: [alpha, beta]}`
- [x] T015 [US2] Implement `buildContagiousIncludeWhenRGD()` in `cmd/dump-fixtures/main.go`: schema `{name: string, enableParent: boolean}`; `WithResource("parentDeploy", deploymentTemplate, nil, []string{"${schema.spec.enableParent}"})` (includeWhen); `WithResource("childConfig", configMapTemplate referencing `${parentDeploy.metadata.name}`, nil, nil)` — child depends on parent via CEL ref
- [x] T016 [US2] Implement `buildContagiousIncludeWhenInstance()` in `cmd/dump-fixtures/main.go`: kind `ContagiousApp`, namespace `kro-ui-e2e`, spec `{name: upstream-contagious, enableParent: false}`
- [x] T017 [US2] Implement `buildClusterScopedRGD()` in `cmd/dump-fixtures/main.go`: `generator.WithSchema("ClusterApp", "v1alpha1", map[string]interface{}{"name":"string","targetNamespace":"string"}, nil, generator.WithScope(krov1alpha1.ResourceScopeCluster))` + `WithResource("appConfig", configMapTemplate with namespace `${schema.spec.targetNamespace}`, nil, nil)`
- [x] T018 [US2] Implement `buildExternalCollectionRGD()` in `cmd/dump-fixtures/main.go`: schema `{name: string, targetNamespace: string}`; `generator.WithExternalRef("teamConfigs", &krov1alpha1.ExternalRef{APIVersion:"v1", Kind:"ConfigMap", Metadata: krov1alpha1.ExternalRefMetadata{Namespace:"${schema.spec.targetNamespace}", Selector: &metav1.LabelSelector{MatchLabels: map[string]string{"role":"team-config"}}}}, nil, nil)`; `WithResource("summary", configMapTemplate referencing `${teamConfigs}`, nil, nil)`
- [x] T019 [US2] Implement `buildExternalCollectionPrereq() []corev1.ConfigMap` in `cmd/dump-fixtures/main.go`: returns 2 `corev1.ConfigMap` objects with `labels: {role: team-config}` in namespace `kro-ui-e2e`; serialise as multi-document YAML (`---` separator) to `upstream-external-collection-prereq.yaml`
- [x] T020 [US2] Implement `buildExternalCollectionInstance()` in `cmd/dump-fixtures/main.go`: kind `ExternalCollectionApp`, namespace `kro-ui-e2e`, spec `{name: upstream-ext-coll, targetNamespace: kro-ui-e2e}`
- [x] T021 [US2] Implement `buildCelComprehensionsRGD()` in `cmd/dump-fixtures/main.go`: schema `{name: string, items: object, scores: object, tags: []string}`; `WithResource("output", configMapTemplate with data fields `transformedItems: "${items.transformMap(k, v, {k: string(int(v) + 10)})}"`, `tagList: "${tags.transformList(i, v, v).join(',')}"`, `tagIndex: "${tags.transformMapEntry(i, v, v, string(i))}"`, nil, nil)`
- [x] T022 [US2] Implement `buildCelComprehensionsInstance()` in `cmd/dump-fixtures/main.go`: kind `CelComprehensionsApp`, namespace `kro-ui-e2e`, spec `{name: upstream-cel-comp, items: {a: "1", b: "2"}, scores: {x: "5"}, tags: [go, kro]}`
- [x] T023 [US2] Wire all 11 builders into `main()` in `cmd/dump-fixtures/main.go`: call each in order, pass result to `writeFixture` with path `filepath.Join(fixturesDir, "upstream-<name>.yaml")`, collect errors, print summary of files written, `os.Exit(1)` if any error
- [x] T024 [US2] Add `dump-fixtures` target to `Makefile` (after the `test-e2e-report` target): `GOPROXY=direct GONOSUMDB="*" go run -tags tools ./cmd/dump-fixtures` with a `## Regenerate upstream fixture YAMLs (run after kro version bump)` comment; also add `dump-fixtures` to the `.PHONY` line
- [x] T025 [US2] Run `make dump-fixtures` and verify 11 files are created in `test/e2e/fixtures/upstream-*.yaml` with Apache 2.0 headers present; commit the generated files

**Checkpoint**: US2 complete — 11 fixture YAMLs committed, tool reproducible.

---

## Phase 5: US3 — E2E setup wiring + fixture-state (Priority: P3)

**Goal**: All 6 new fixture families applied in `global-setup.ts` and `demo.sh`; 6 new readiness flags written to `fixture-state.json`.

**Independent test**:
```bash
cat test/e2e/fixture-state.json
# → all 10 keys present (4 existing + 6 new)
```

**Prerequisites**: T025 (YAML files must be committed before wiring in setup).

- [x] T026 [US3] Edit `test/e2e/fixture-state.ts`: add `cartesianReady`, `collectionChainReady`, `contagiousReady`, `clusterScopedReady`, `externalCollectionReady`, `celComprehensionsReady` to `FixtureState` interface and both fallback return objects in `loadFixtureState()`
- [x] T027 [US3] Edit `test/e2e/setup/global-setup.ts`: extend `fixtureState` initialiser object with all 6 new keys set to `false`
- [x] T028 [US3] Edit `test/e2e/setup/global-setup.ts` — add step 6g: apply `upstream-cartesian-foreach-rgd.yaml`, best-effort wait `condition=Ready` 180s, apply `upstream-cartesian-foreach-instance.yaml`, set `fixtureState.cartesianReady` on success (follow existing `try/catch` pattern from step 6b)
- [x] T029 [US3] Edit `test/e2e/setup/global-setup.ts` — add step 6h: apply `upstream-collection-chain-rgd.yaml`, wait 180s, apply instance, set `fixtureState.collectionChainReady`
- [x] T030 [US3] Edit `test/e2e/setup/global-setup.ts` — add step 6i: apply `upstream-contagious-include-when-rgd.yaml`, wait 120s, apply instance, set `fixtureState.contagiousReady`
- [x] T031 [US3] Edit `test/e2e/setup/global-setup.ts` — add step 6j: apply `upstream-cluster-scoped-rgd.yaml`, wait 120s (no instance apply), set `fixtureState.clusterScopedReady`
- [x] T032 [US3] Edit `test/e2e/setup/global-setup.ts` — add step 6k: apply `upstream-external-collection-prereq.yaml`, then apply `upstream-external-collection-rgd.yaml`, wait 120s, apply `upstream-external-collection-instance.yaml`, set `fixtureState.externalCollectionReady`
- [x] T033 [US3] Edit `test/e2e/setup/global-setup.ts` — add step 6l: apply `upstream-cel-comprehensions-rgd.yaml`, wait 120s, apply `upstream-cel-comprehensions-instance.yaml`, set `fixtureState.celComprehensionsReady`
- [x] T034 [US3] Edit `scripts/demo.sh` — add apply blocks for all 6 new fixture families (+ prereq for external-collection) using `wait_rgd_ready <name> 180s optional` pattern; apply instance YAMLs with `2>/dev/null || true` where optional

**Checkpoint**: US3 complete — all new fixtures wired into both E2E setup and demo.

---

## Phase 6: US4 — New Playwright journeys (Priority: P4)

**Goal**: 6 new journey files assert correct kro-ui rendering for every newly added kro feature.

**Independent test**: `cd test/e2e && bunx playwright test --grep "043"` — all steps pass or skip gracefully.

**Prerequisites**: T026 (fixture-state keys must exist).

- [x] T035 [P] [US4] Create `test/e2e/journeys/043-cartesian-foreach.spec.ts`: Apache 2.0 header; import `fixtureState`; Step 1: `test.skip(!fixtureState.cartesianReady, …)` — DAG renders for `/rgds/upstream-cartesian-foreach`, `dag-svg` visible; Step 2: collection node `dag-node-appConfigs` visible; Step 3: two forEach annotations visible (`[data-testid^="dag-node-foreach-"]` count ≥ 2) with text containing "region" and "tier"; Step 4: navigate to live instance, `collection-badge` visible with text matching `/^[1-4]\/4$|^4$/` (up to 4 while reconciling)
- [x] T036 [P] [US4] Create `test/e2e/journeys/043-collection-chain.spec.ts`: Apache 2.0 header; `test.skip(!fixtureState.collectionChainReady, …)`; Step 1: DAG renders for `/rgds/upstream-collection-chain`; Step 2: `dag-node-baseConfig` visible with no `dag-node--collection` class (it's a resource node); Step 3: `dag-node-chainedConfigs` visible with `dag-node--collection` class; Step 4: at least one `dag-edge` present (dependency edge from baseConfig to chainedConfigs); Step 5: live instance `collection-badge` on chainedConfigs node visible
- [x] T037 [P] [US4] Create `test/e2e/journeys/043-contagious-include-when.spec.ts`: Apache 2.0 header; `test.skip(!fixtureState.contagiousReady, …)`; Step 1: DAG renders for `/rgds/upstream-contagious-include-when`; Step 2: `dag-node-parentDeploy` visible — has `dag-node--conditional` class or `node-conditional` class (excluded node indicator); Step 3: `dag-node-childConfig` visible — also has excluded indicator (contagious exclusion); Step 4: neither `dag-node-parentDeploy` nor `dag-node-childConfig` has any error-state CSS class (`dag-node--error`, `status-error`, etc.)
- [x] T038 [P] [US4] Create `test/e2e/journeys/043-cluster-scoped.spec.ts`: Apache 2.0 header; `test.skip(!fixtureState.clusterScopedReady, …)`; Step 1: `upstream-cluster-scoped` card visible on `/` or `/catalog`; Step 2: DAG renders on `/rgds/upstream-cluster-scoped`; Step 3: navigate to `/rgds/upstream-cluster-scoped?tab=instances` — either empty state shown (no instance applied, expected) or if row present, namespace column is empty/blank for the cluster-scoped CR
- [x] T039 [P] [US4] Create `test/e2e/journeys/043-external-collection.spec.ts`: Apache 2.0 header; `test.skip(!fixtureState.externalCollectionReady, …)`; Step 1: DAG renders for `/rgds/upstream-external-collection`; Step 2: a node with CSS class `dag-node--external-collection` OR `dag-node--external` is visible (soft-assert: if only `dag-node--external` class found, log potential UI feature gap for `NodeTypeExternalCollection` distinction); Step 3: click the external collection node → node detail panel opens; Step 4: panel contains "External ref" or "External ref collection" type text (no `?` in type badge); Step 5: node label is not `?` and not empty
- [x] T040 [P] [US4] Create `test/e2e/journeys/043-cel-comprehensions.spec.ts`: Apache 2.0 header; `test.skip(!fixtureState.celComprehensionsReady, …)`; Step 1: DAG renders for `/rgds/upstream-cel-comprehensions`; Step 2: navigate to YAML tab (`?tab=yaml`); Step 3: at least one `token-cel-expression` span visible; Step 4: page text contains `transformMap` or `transformList` (the raw CEL function names appear in the highlighted YAML); Step 5: no `?` or `undefined` text anywhere in the token spans

**Checkpoint**: US4 complete.

---

## Phase 7: US5 — Journey coverage upgrades (Priority: P5)

**Goal**: Journeys 005, 006, and 017 assert specific kro behaviours instead of "no crash".

**Independent test**: Run `cd test/e2e && bunx playwright test 005 006 017` — upgraded steps pass.

- [x] T041 [US5] Edit `test/e2e/journeys/006-cel-highlighting.spec.ts`: add `import { fixtureState } from '../fixture-state'` (if not already present); add `test.skip(!fixtureState.celFunctionsReady, 'cel-functions RGD not Ready in setup')` to Step 5 (navigates to `/rgds/cel-functions`) and Step 6 (asserts `.split(` and `json.marshal` tokens); Step 9 (optional-chaining `orValue` assertion on `external-ref`) already handles missing fixture gracefully — verify it uses soft guard or add `test.skip(!fixtureState.externalRefReady, …)` if not already guarded
- [x] T042 [US5] Edit `test/e2e/journeys/017-rgd-validation-linting.spec.ts`: after the existing assertion that `validation-tab` renders without crashing, add Step 6: `const conditionTypes = page.locator('[data-testid^="condition-type-"], .condition-item__type, [class*="condition-type"]')` — if visible, assert at least one has text content matching `/GraphAccepted|Ready|KindReady|ControllerReady/`; soft-pass if no condition type elements present (log "no typed condition elements found — kro version may not emit conditions")
- [x] T043 [US5] Edit `test/e2e/journeys/005-live-instance.spec.ts`: in the step that opens the conditions panel (Step 8 or 9), after confirming the panel is visible, add assertion: find condition entries matching `.condition-item` or `[data-testid^="condition-item-"]`; if count > 0, assert at least one has a non-empty `reason` text (`/Ready|Reconciling|Pending|[\w]+/`); assert none contain text `undefined` or `[object Object]`; soft-pass if no condition items present

**Checkpoint**: US5 complete — journeys 005, 006, 017 upgraded.

---

## Phase 8: Polish & Validation

- [x] T044 Run `go vet ./...` — must exit 0
- [x] T045 Run `cd web && bun run typecheck` — must exit 0
- [x] T046 [P] Run `go build ./cmd/kro-ui` (no `-tags tools`) — must succeed; confirm kro dep not in binary
- [x] T047 [P] Fixture parity audit: `ls test/e2e/fixtures/*-rgd.yaml` — every file referenced in both `global-setup.ts` and `scripts/demo.sh`
- [x] T048 [P] Run `make dump-fixtures` twice; `git diff test/e2e/fixtures/upstream-*.yaml` must be empty after second run (idempotency check)
- [x] T049 [P] Verify journey 025 Step 4 no longer has an early-return path before reaching the `chain-cycle-a` RGD URL (fixture now applied)
- [x] T050 [P] Count `test/e2e/journeys/*.spec.ts` — expect 42 files (36 existing + 6 new)

---

## Dependencies & Execution Order

### Phase dependencies

- **Phase 1**: No deps — start immediately
- **Phase 2**: Depends on Phase 1 — BLOCKS all Go tool work
- **Phase 3 (US1)**: Independent of Phase 2 — parallel with Phase 2
- **Phase 4 (US2)**: Depends on Phase 2 (Go module)
- **Phase 5 (US3)**: Depends on Phase 4 T025 (YAMLs must be committed)
- **Phase 6 (US4)**: Depends on Phase 5 T026 (fixture-state keys must exist)
- **Phase 7 (US5)**: Independent of Phases 4–6 — can run any time after Phase 1
- **Phase 8**: Depends on all phases complete

### User story dependencies

- **US1**: Independent — can commit alone
- **US2**: Depends on Phase 2
- **US3**: Depends on US2 T025
- **US4**: Depends on US3 T026
- **US5**: Independent of US2–US4

### Within each phase — parallel opportunities

| Parallel group | Tasks |
|----------------|-------|
| Phase 3 | T007 ‖ T008 (different files) |
| Phase 4 | T011–T022 sequential in same file; T024 (Makefile) ‖ main file edits |
| Phase 5 | T028–T033 sequential (same file `global-setup.ts`); T034 (`demo.sh`) ‖ any of T028–T033 |
| Phase 6 | T035 ‖ T036 ‖ T037 ‖ T038 ‖ T039 ‖ T040 (all different files) |
| Phase 7 | T041 ‖ T042 ‖ T043 (different files) |
| Phase 8 | T044, T045 sequential; T046 ‖ T047 ‖ T048 ‖ T049 ‖ T050 |

---

## Implementation Strategy

### MVP commit sequence

```
1. commit: test(e2e): apply chain-cycle fixtures in E2E setup and demo        [US1 = T007+T008]
2. commit: build(tools): add cmd/dump-fixtures — upstream kro fixture gen     [US2 = T009–T025]
3. commit: test(e2e): wire 6 upstream fixture families into E2E setup + demo  [US3 = T026–T034]
4. commit: test(e2e): add 6 journeys for upstream kro feature coverage        [US4 = T035–T040]
5. commit: test(e2e): upgrade journeys 005/006/017 condition assertions        [US5 = T041–T043]
```

### Final state after all tasks complete

| Metric | Before | After |
|--------|--------|-------|
| Journey files | 36 | 42 |
| Journey steps (approx) | 219 | ~275 |
| Fixture families | 7 (1 unapplied) | 14 (all applied) |
| kro node types covered | 4/5 (`NodeTypeExternalCollection` missing) | 5/5 |
| kro upstream features without UI coverage | 4 major | 0 |
| Dead `fixtureState` keys | 1 (`celFunctionsReady`) | 0 |
| Journeys with "no crash" only assertions | 3 (005, 006, 017) | 0 |
