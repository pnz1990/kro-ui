apiVersion: v1
kind: Service
metadata:
  name: {{ include "kro-ui.fullname" . }}
  labels:
    {{- include "kro-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kro-ui.selectorLabels" . | nindent 4 }}
