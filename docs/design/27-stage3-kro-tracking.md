# 27 — Stage 3: kro Upstream Tracking + Next Features

> Status: Active | Created: 2026-04-20
> Applies to: kro-ui ongoing development (post-v0.9.4)

---

## Problem

kro-ui is mature at v0.9.4. The roadmap says "Track kro upstream releases and apply UI
changes as new CRDs/fields land" but there is no concrete queue backing this. Without
explicit Future items, the loop generates busywork (test coverage micro-PRs) instead of
meaningful product advancement.

This design doc provides the concrete queue for Stage 3.

---

## kro Upstream Tracking

kro v0.9.1 is the current supported version. The kro project is actively developed.
Each new kro release may add new CRD fields, new health states, new CEL functions,
or new ResourceGraphDefinition capabilities that kro-ui must reflect.

**Protocol**: Check `kubernetes-sigs/kro` releases before every batch. If a new
release has landed since our last check:
1. Read the release notes and changelog
2. Identify any CRD schema changes, new fields, or new API paths
3. Open a `feat(kro-vX.Y.Z): support new <feature>` issue per change
4. Implement the UI changes in the normal COORD→ENG→QA loop

---

## Present

✅ 27.0 — kro v0.9.1 support: GraphRevision CRD, hash column, CEL hash functions (PR #428)
✅ 27.1 — kro release tracking automation: `.github/workflows/kro-upstream-check.yml` weekly checks `kubernetes-sigs/kro` releases/latest; if newer than go.mod version, opens `feat(kro-vX.Y.Z)` issue automatically. `otherness-config.yaml` configured with `anchor.upstream_version_file`+`pattern` for local go.mod bump detection via SM §4g-anchor-upstream. (PR #545, issue #523)
 ✅ 27.2 — Accessibility pass: journey `074-accessibility.spec.ts` registered in Playwright chunk-9 testMatch pattern; axe-core WCAG 2.1 AA scan runs on Catalog, RGD DAG, Instance list, and Context switcher pages in CI. (PR #546, issue #529)
 ✅ 27.3 — Fleet persona anchor journey: 6-step journey covering multi-cluster fleet view → health matrix → context switch → per-cluster RGD count (journey 075, issue #524)
 ✅ 27.6 — Error state coverage: E2E journeys 076-079 added and registered in Playwright chunk-9; each uses `page.route()` to mock 5xx API responses and asserts the error state element is visible on Overview, Fleet, RGD detail, and Instance detail pages. (PR TBD, issue #531)
 ✅ 27.4 — Performance budget: `.github/workflows/perf.yml` Lighthouse CI check (score ≥ 50, calibrated for 521KB bundle on GA runners) + HTTP response time check (<500ms); E2E journey `080-performance-budget.spec.ts` asserts Overview DOM Interactive ≤1000ms and content-ready ≤1500ms; registered in Playwright chunk-9. (PR #549, issue #530)
 ✅ 27.7 — Donation readiness: `OWNERS` file (kubernetes-sigs format, approvers/reviewers: pnz1990); `.github/workflows/dco.yml` enforces DCO sign-off on all PRs; `CONTRIBUTING.md` updated with DCO section explaining `git commit -s` requirement. (issue #532)
 ✅ 27.15 — release.yml stale helm/ reference removed: the `Update and push Helm chart` step that references `helm/kro-ui` (removed in v0.9.4) has been deleted from `.github/workflows/release.yml`; v0.10.0 tag push will now succeed. (PR #588, issue #587)
 ✅ 27.5 — kro-ui v0.10.0 release: GitHub release with changelog from merged PRs since v0.9.4; goreleaser produces binary archives (Linux/Darwin/Windows amd64/arm64) and Docker image pushed to GHCR; release notes auto-generated from PR titles. (PR #589, issue #525)
 ✅ 27.8 — GOVERNANCE.md: lightweight governance model (kubernetes-sigs format — maintainers list, decision process, release managers, path to reviewer/approver); GOVERNANCE.md added at repo root. (issue #570)
 ✅ 27.9 — CODE_OF_CONDUCT.md at repo root: pointer file to Kubernetes Community CoC with reporting path and enforcement summary; satisfies kubernetes-sigs org requirement. (issue #571)
 ✅ 27.11 — OWNERS breadth: OWNERS now documents the path for community members to become reviewers; GOVERNANCE.md §Becoming a Reviewer/Approver added; comment in OWNERS noting the >= 2 approver requirement. (issue #573)

---

## Future

- ✅ 27.10 — Supply chain security for releases: cosign keyless signing of container images, CycloneDX SBOM attached via `anchore/sbom-action` + `cosign attach sbom`, SLSA provenance attestation via `cosign attest`, goreleaser `sboms:` block for binary archives, `id-token: write` permission added to release job. (spec issue-572, 2026-04)
- ✅ 27.12 — Partial-RBAC / restricted-namespace testing: `ListAllInstances` tracks Forbidden errors via atomic counter and returns `rbacHidden` in the response; `ListInstances` returns 200 + `{"items":[],"warning":"insufficient permissions"}` on Forbidden instead of 500; `/instances` page and RGD detail Instances tab show "N RGDs hidden — insufficient permissions" when rbacHidden > 0; Go unit tests cover both handlers; E2E journey 081. (spec issue-574, 2026-04)
- ✅ 27.13 — DAG scale guard: RGDs with >100 nodes show a text-mode node list by default instead of a dense SVG that locks up the browser; a "Show graph (N nodes — may be slow)" toggle allows opt-in to the full render; nodes are grouped by type with conditional badges; DAGScaleGuard wraps DAGGraph and StaticChainDAG. (PR #613, spec issue-575)
- ✅ 27.14 — Frontend code splitting: route-based React.lazy + Suspense per route reduces initial bundle from ~521KB to vendor + entry chunks (~150KB gzipped); perf.yml threshold raised from 50 to 70; PageLoader shimmer fallback renders within Layout shell while chunks load. (PR #612, spec issue-576)
- ✅ 27.16 — Google Fonts external dependency removal: self-hosted Inter (300–700) and JetBrains Mono (400–600) WOFF2 files in `web/public/fonts/`; `@font-face` declarations in `web/public/fonts.css`; `index.html` no longer references `fonts.googleapis.com` or `fonts.gstatic.com`; binary embeds fonts via existing `go:embed all:dist` (PR #644, 2026-04)
- 🔲 27.17 — OS-preference light mode: `tokens.css` defines `[data-theme="light"]` but nothing in the app reads `window.matchMedia('(prefers-color-scheme: light)')` or exposes a theme toggle; users whose OS is in light mode see the dark theme; add a `useTheme()` hook that reads the OS preference on mount and syncs `document.documentElement.setAttribute('data-theme', ...)`, with a local-storage override for manual toggling; WCAG 2.1 SC 1.4.3 contrast ratios are calibrated per mode — the wrong mode can fail contrast requirements
- ✅ 27.18 — E2E scale fixture: `scale-test-rgds.yaml` defines `scale-wide` RGD with 20 ConfigMap resource nodes (largest in CI fixture corpus); `scale-test-instances.yaml` applies one `ScaleWideApp` CR; journey 083 verifies Overview DOM Interactive ≤2000ms with the scale RGD present and the DAG SVG renders ≥20 nodes without JS errors (dagre/stack overflow guard); `fixtureState.scaleReady` flag guards all tests; added to chunk-9 in playwright.config.ts (PR #issue-663, 2026-04)
- 🔲 27.19 — E2E slow-API / fetch-timeout scenario: the error-state journeys (076-079) mock HTTP 5xx responses but never simulate a slow or aborted fetch (e.g. a cluster that accepts the TCP connection but does not respond for 10s); add a journey that uses `page.route()` with a 12s `delayMs` to verify (a) the UI shows a loading indicator during the delay, (b) the timeout fires and shows a user-readable error, and (c) the retry button triggers a new fetch; this tests the `AbortController`/`AbortSignal` plumbing that exists in the frontend but is currently untested in E2E; a kubernetes-sigs reviewer would ask "what happens when the API hangs?"
- ✅ 27.20 — Frontend per-request fetch timeout: `withTimeout()` helper in `web/src/lib/api.ts` wraps every `get()` and `post()` call with `AbortSignal.any([callerSignal, AbortSignal.timeout(30_000)])`; graceful fallback for environments without `AbortSignal.any`; symmetrical with the 30s server-side route deadline (PR #645, 2026-04)
- ✅ 27.21 — Fleet per-cluster inner deadline: `summariseContext()` in `fleet.go` now adds `context.WithTimeout(parent, 5*time.Second)` as its first statement; the errgroup fan-out uses the bounded context; `TestFleetSummaryHandler_ContextTimeout` verifies the response completes within 6s when a cluster's `List()` hangs indefinitely (PR #646, 2026-04)
- ✅ 27.22 — release.yml `id-token: write` permission added (prerequisite for cosign keyless signing). Addressed as part of spec issue-572 (27.10), 2026-04.
- 🔲 27.23 — SECURITY.md post-donation update: the current `SECURITY.md` references `pnz1990/kro-ui` GitHub Security Advisories; when donated to kubernetes-sigs, all security reports must be routed through the kubernetes-sigs security process (`security@kubernetes.io`); add a `## Post-Donation Security Policy` section that documents the planned update so it is not overlooked during the donation PR review; a kubernetes-sigs maintainer reviewing the donation PR will check SECURITY.md and flag an incorrect reporting URL as a blocker
- 🔲 27.24 — Community outreach for second OWNERS approver: `OWNERS` has a single approver (`pnz1990`); kubernetes-sigs rejects donations with a single-person bus factor and requires >= 2 approvers before accepting a project; `GOVERNANCE.md` documents the path to becoming a reviewer/approver but there is no concrete outreach plan; open a GitHub discussion or email the `kubernetes-sigs/kro` maintainer list to identify a candidate from the kro community who can review 3+ PRs and qualify as approver before the donation PR is filed; this is a social/process gap, not a code gap — it takes weeks, not days

---

## System Loop Health (🔲 Future)

These items address the *development system itself* — gaps identified by applying the 5 pressure lenses
(reliability, loop honesty, self-improvement, onboarding, visibility) to the current otherness loop for kro-ui.
They are not product features; they are process improvements the loop must make to stay honest.

- 🔲 27.22 — Loop reliability: housekeeping-only batch detection — batches 3–8 in the metrics table shipped exclusively test-coverage micro-PRs (one per batch, ~1% coverage each); the loop posted GREEN health signal throughout; add a check to `standalone.md` SM phase that inspects `prs_merged` vs `todo_shipped` type: if ≥3 consecutive batches ship only test coverage or docs with no design-doc-backed feature PRs, the SM must post AMBER and self-assign a `feat:` issue from the open queue before the batch ends; a truly reliable system must self-diagnose and self-correct busywork spirals without human intervention
- 🔲 27.23 — Loop honesty: SM health signal accuracy — the SM posts GREEN/AMBER/RED but GREEN has no quantitative threshold; a batch that ships `prs_merged=1` (coverage micro-PR) with `todo_shipped=1` is structurally identical to a batch that ships a major feature; add explicit thresholds to the health signal: GREEN requires ≥1 design-doc-backed feature PR merged; AMBER if only hygiene/test PRs shipped; RED if CI was broken >2h or 0 PRs merged; post these thresholds visibly in the report so the human can verify honesty without reading every PR
- 🔲 27.24 — Self-improvement: `/otherness.learn` frequency gate — the metrics table has 9 rows over 2 days with `skills_count` stuck at 12 for all 7 rows where it is reported; `/otherness.learn` is not running between sessions; add a rule to `standalone.md` SM phase: if `skills_count` has not increased in the last 5 batches, automatically invoke `/otherness.learn` targeting the top 3 open GitHub issues by comment count (proxy for "most discussed, most complex") before the next batch; the skills library must grow or the loop is not learning
- 🔲 27.25 — Self-improvement: monoculture / frame-lock detection — all sessions use the same `standalone.md` reasoning chain (COORD→ENG→QA→SM→PM); there is no mechanism to detect when this chain produces systematically wrong outputs (e.g. all QA passes that later turn out to have bugs, or all COORD specs that miss user-visible value); add a monthly `/otherness.arch-audit` invocation triggered by the SM when `prs_merged` in the last 30 days exceeds 20 but no new `🔲 Future` items have been added to design docs by the loop itself (a sign that the loop is executing but not discovering); the arch-audit should be instructed to assume the current approach is wrong and look for evidence
- 🔲 27.26 — Visibility: single-page loop health dashboard — issue #439 is the report issue but its comment stream is dense and technical (batch IDs, session hashes, coverage percentages); a human glancing at the repo today cannot answer in <30 seconds: (1) is the loop healthy? (2) what meaningful feature shipped most recently? (3) is the product moving toward donation readiness?; add a `docs/aide/loop-health.md` file that the SM updates on every batch with a 3-line quick-glance section (status, last feature PR, next priority) followed by the full metrics table; pin a link to this file in the issue #439 body so it is the first thing a human sees; the vibe-vision-auto scan should promote this to ✅ once the SM is consistently updating it
- 🔲 27.27 — Onboarding: zero-human-intervention validation — `otherness.setup.md` and `otherness.onboard.md` exist but their output quality is untested; add a validation step: once per quarter, run `/otherness.onboard` on a scratch repo (or use a dedicated `test-onboard` branch in kro-ui) and assert the result is a fully working `otherness-config.yaml` with no manual edits required; if human edits were needed, open a bug against the onboarding command and fix it before the next session; the goal is that a new project added today is running its first autonomous batch within 1 hour with zero human intervention
- 🔲 27.28 — Reliability: silent session failure detection — a session that runs to completion but produces 0 merged PRs and 0 issue comments (not even a status update) is indistinguishable from a session that never ran; the current health signal only reports on batches that successfully post to issue #439; add a circuit-breaker in the SM phase: if the session ends with `prs_merged=0` AND no comment was posted to the report issue, emit a FAILED status comment before exiting with the last error seen; GitHub Actions should surface this as a workflow annotation so the human sees it immediately; silent failures are the most dangerous because the human assumes the loop is running when it is not
- 🔲 27.29 — Reliability: empty-queue sentinel behavior — when the COORD phase finds no open issues on the board (queue empty), the current behavior is to stall or fall back to hygiene/coverage micro-PRs which does not advance the product; add an explicit empty-queue protocol to `standalone.md`: (1) check for any open GitHub issues (not just board items) labeled with the project label; (2) if none, run `vibe-vision-auto` to generate new items from code gaps; (3) if still none, post a "queue drained — new items needed" comment to the report issue and exit cleanly; the loop must never silently spin on empty input — it must either self-fill the queue or clearly signal that human direction is needed
- 🔲 27.30 — Self-improvement: simulation prediction feedback — the metrics table (`docs/aide/metrics.md`) records `prs_merged`, `skills_count`, and `todo_shipped` but there is no record of what the SM *predicted* would ship at batch start vs. what actually shipped; without this delta, the simulation cannot self-correct: the same optimistic predictions recur every batch regardless of actual track record; add a `predicted_prs` column to the metrics table that the COORD phase fills at session start (based on queue depth and item size), and require the SM to compute and record `accuracy = actual/predicted`; if accuracy is below 0.5 for 3 consecutive batches, the SM must halve its next prediction and post the reason — predictions that never change are not predictions, they are placeholders
- 🔲 27.31 — Self-improvement: learning velocity metric — `skills_count` is recorded in the metrics table but it is the total count, not the rate of growth; a skills library with 12 skills that has not grown in 9 batches is effectively stagnant; add a `skills_delta` column to the metrics table showing the change from the previous batch; add a SM rule: if `skills_delta=0` for 5 consecutive batches, the SM must (1) report the specific types of errors or rework that occurred in those batches and (2) propose a concrete new skill that would have prevented them; the goal is that the agents are demonstrably smarter (can solve problems faster with fewer retries) than they were 2 weeks ago — without a velocity metric this claim cannot be verified or falsified
- 🔲 27.32 — Visibility: SM must write `docs/aide/loop-health.md` every batch — item 27.26 specifies adding a `docs/aide/loop-health.md` quick-glance dashboard and having the SM update it, but the SM phase `§4f` has no code to write this file; `docs/aide/loop-health.md` does not exist today despite 27.26 being in the Future queue; add an explicit write step to SM §4f that creates/updates `docs/aide/loop-health.md` with: (1) 3-line quick-glance (current health, last feature PR, next priority), (2) donation readiness progress (open blockers count), and (3) the last 10 rows of metrics; the file must be committed on every batch so vibe-vision-auto can promote 27.26 to ✅; without this step 27.26 can never self-promote because the evidence it requires is never written
- 🔲 27.33 — Reliability: `cancel-in-progress: true` kills in-flight sessions mid-work — the scheduled workflow uses `concurrency: cancel-in-progress: true`; an hourly cron trigger that fires while a 2-hour session is running at minute 55 will silently kill the session before SM §4f can post its health signal or commit metrics; this produces exactly the silent-failure pattern 27.28 is meant to prevent — but from an infrastructure cause, not a code cause; evaluate changing to `cancel-in-progress: false` (queue the new run) or adding a pre-exit hook that the cancel signal triggers; the current setup means any session that runs for >60 minutes has a guaranteed kill before completion
- 🔲 27.34 — Honesty: SM-computed `skills_count` must reflect actual file count — the metrics table has `skills_count=12` across all historical rows, but `ls ~/.otherness/agents/skills/ | wc -l` currently returns 15; the SM is writing a stale or hardcoded value rather than computing the count at batch time; add an explicit `SKILLS_COUNT=$(ls ~/.otherness/agents/skills/*.md 2>/dev/null | wc -l)` computation in SM §4b before writing the metrics row; without this, the `skills_delta` calculation introduced in 27.31 will compute deltas from wrong baselines and the learning velocity signal will be misleading
- 🔲 27.35 — Honesty: `predicted_prs` and `skills_delta` columns added to metrics header but SM never writes them — PR #604 added a comment block to `docs/aide/metrics.md` documenting the new `predicted_prs` and `skills_delta` columns, but the SM phase code in `~/.otherness/agents/phases/sm.md` was not updated to populate them; every row written after PR #604 has `-` in both new columns, rendering the columns decorative rather than functional; update SM §4b to (1) compute PREDICTED_PRS from COORD queue depth at session start and write it at the beginning of the batch, and (2) compute SKILLS_DELTA as current_count minus the prior row's count and include it in the metrics row write
- 🔲 27.36 — Visibility: `docs/aide/vision.md` donation readiness table updated manually — the "Donation Readiness Gap Analysis" section in `docs/aide/vision.md` was written manually and is not updated by any automated scan; as gaps are resolved (e.g. GOVERNANCE.md, CODE_OF_CONDUCT.md shipped in PR #592) the table should move items from the gap list to "Already addressed"; the vibe-vision-auto scan should check each hard-blocker and significant-gap row against the merged PR list and move resolved items to the "Already addressed" section, rather than requiring a human to read every PR and update the table by hand
- 🔲 27.37 — Honesty: metrics table audit trail gap — `docs/aide/metrics.md` has batch numbers 1–8 then jumps to batch 51 with 43 consecutive batches unrecorded; the SM is either not writing rows or writing them to an unreachable location; a reader cannot determine whether those 43 batches shipped meaningful work or were all busywork; add a SM rule: if the current batch number differs from the last recorded batch by more than 2, the SM must write a gap row explicitly noting the unrecorded range with `notes="N batches unrecorded — see session logs"` before writing the current row; without a continuous audit trail the metrics table is not a reliable signal — it is a selected sample that could conceal sustained stagnation
- 🔲 27.38 — Self-improvement: rework rate tracking — the metrics table has no `rework_prs` column; a rework PR is defined as any `fix(*)` or `chore(*)` PR whose title references a feature or spec shipped in the same or immediately prior batch (e.g. a hotfix for a bug introduced in the same session); high rework rate is the primary signal that first-pass quality is degrading and that a new skill is needed; add a `rework_prs` column to the metrics table; in SM §4b, count any PR merged in the current batch whose title starts with `fix` and whose referenced issue or PR number was also opened in the current batch; add a SM rule: if `rework_prs >= prs_merged * 0.3` for 2 consecutive batches, invoke `/otherness.learn` targeting the most-reworked spec type — the agents are producing low-quality first passes at an unacceptable rate
- 🔲 27.39 — Onboarding: new project gets no initial pressure context — when `/otherness.onboard` creates a new project's `otherness-scheduled.yml`, the `prompt:` block for Step A (vibe-vision-auto) contains only the generic instruction to read the workflow file; there is no project-specific pressure block; the system therefore starts a new project's first scan with zero diagnostic context — it has no idea what the product is meant to accomplish or where its gaps are; add a step to `/otherness.onboard` that generates a minimal initial pressure block from the project's README and first design doc (or `otherness-config.yaml` description field), inserted into the Step A prompt as the initial "Context for this vision scan:" section; a new project that starts with meaningful pressure will self-direct from day one instead of requiring a human to write the first pressure prompt
- 🔲 27.40 — Visibility: `vision.md` donation readiness table goes stale after every shipping batch — PR #626 shipped cosign signing + SBOM (27.10) on 2026-04-21 but `docs/aide/vision.md` still lists "No signed artifacts or SBOM in release" as a hard blocker with no strikethrough; PR #622 shipped partial-RBAC (27.12) but the significant-gap row still shows it as open; the vibe-vision-auto SCAN 1 only updates `docs/design/*.md` `🔲` items and never touches `docs/aide/vision.md`; add a Scan 6 to `vibe-vision-auto.md` that parses the hard-blocker and significant-gap table rows in `docs/aide/vision.md`, matches each against merged PR titles and referenced design doc items, and marks resolved rows with `~~strikethrough~~ ✅ shipped (PR #N)` — the donation readiness table must be self-maintaining or it will always lag reality and mislead a human reviewer doing final donation checks
- 🔲 27.41 — Reliability: partial-work visibility when session is killed — `cancel-in-progress: true` will kill a session mid-flight (27.33 covers the architecture decision), but there is currently no mechanism to make partial work visible after a kill; if a session completes ENG but not QA before being cancelled, the opened PR sits unmerged indefinitely with no comment explaining why SM never ran; add a GitHub Actions composite action (or a `trap SIGTERM` in the scheduled workflow entrypoint) that catches the cancellation signal and posts a "session cancelled mid-work — last phase: N" comment to the report issue with a link to the open PR; without this, cancelled sessions are indistinguishable from successful ones and unmerged PRs accumulate silently
- 🔲 27.42 — Honesty: metrics backfill audit when gap is detected — item 27.37 adds a SM rule to write a gap sentinel row when batch numbers are non-contiguous; but writing "43 batches unrecorded" is not the same as understanding what happened in those batches; when a gap is detected, the SM should attempt a backfill by querying `gh pr list --state merged` for the gap date range, computing estimated `prs_merged` and `vision_prs` per day, and writing summarized reconstruction rows marked with `(reconstructed)`; this transforms the audit trail from "we know there's a hole" to "we have an estimate of what was in the hole" — the difference between an opaque gap and a recoverable one
- 🔲 27.43 — Onboarding: pressure block in a separate doc instead of inline YAML — the Step A pressure block is currently embedded inline in `otherness-scheduled.yml` as a multi-line YAML string; updating the pressure context requires editing a workflow file (which triggers stricter review, requires `contents: write` to push, and is easy to corrupt with YAML indentation errors); extract the pressure block to `docs/design/vision-pressure.md` and have the workflow `cat` it into the prompt at runtime; this makes pressure updates a doc PR (no YAML risk) and makes the pressure block visible as a first-class doc that the vibe scan can evolve over time; new projects created by `/otherness.onboard` get a well-structured pressure file rather than an embedded blob
- 🔲 27.44 — Visibility: SM batch complete comment is too technical for a human skimming issue #439 — the current SDM comment format is `[🔄 SDM | sess-412931da | otherness@5c43083] Batch 1 complete. Health: GREEN | Queue: 0 todo 0 in_review | Action: Queue empty — generating next cycle`; this contains useful data but no human-readable narrative; a maintainer checking the issue stream cannot quickly answer "did anything meaningful ship this session?"; prepend a one-line plain-English summary to the SDM comment: "Shipped: <list of feature PR titles, comma-separated, or 'no feature PRs'> | Health: GREEN/AMBER/RED | Next: <next queue item title or 'queue empty'>"; the technical session hash and counters remain but follow the plain-English line; this is the minimum change needed for the issue stream to be readable without clicking every PR link
- 🔲 27.45 — Self-improvement: skill creation trigger on `chore-only` classification — SM §4b detects `SESSION_OUTCOME=chore-only` (implemented in sm.md) and opens a `[DEFECT]` issue, but the defect issue description says "run /otherness.vibe-vision"; it does not trigger `/otherness.learn` even though a chore-only session is strong evidence that the agents lack a skill for the next feature type; add a rule: when `SESSION_OUTCOME=chore-only` for 2 consecutive sessions AND `skills_delta=0`, the SM must invoke `/otherness.learn --context="last 3 issues by comment count"` rather than just filing a defect issue; filing an issue documents the problem; running `/otherness.learn` is the only action that makes the next session smarter
- 🔲 27.46 — Reliability: COORD must refuse hygiene work when feature issues are open — the busywork-spiral detection in 27.22 operates at SM level (after the batch is complete); but the root cause is that COORD picks hygiene/coverage work from the queue without checking whether any design-doc-backed `feat:` issues are open; add an explicit check at the start of the COORD `§2 Pick` step: if `gh issue list --label <PR_LABEL> --state open` returns any issue whose title starts with `feat:` or `fix:` (i.e. is not `chore:`/`docs:`/`test:`), COORD MUST pick one of those issues and MUST NOT pick a hygiene issue; this prevents the chore-spiral before it starts rather than detecting it after 3 consecutive batches; a truly reliable system ships at least one meaningful item every single run without exception — enforcement must be at decision time, not retrospectively
- 🔲 27.47 — Honesty: spec complexity feedback — when `predicted_prs` misses by >50% for 2 consecutive batches (implemented via 27.30), the current SM rule halves the next prediction number; this treats the symptom (over-prediction) but not the root cause (COORD is scoping specs too broadly for a single session); add a second SM rule: when prediction accuracy < 0.5 for 2 batches, COORD must in the next session scope each spec to a maximum of 2 files changed and 1 test file added; post a "scope-constrained session" notice to the report issue; lift the constraint once accuracy returns to >= 0.5 for 2 consecutive batches; without scoping feedback, the loop will oscillate between over-ambitious specs and under-delivering sessions indefinitely — prediction tracking alone does not break the cycle
- 🔲 27.48 — Visibility: vibe-vision-auto must detect a stale `docs/aide/loop-health.md` — items 27.26 and 27.32 specify that `docs/aide/loop-health.md` must be written by SM every batch and that the vibe scan promotes 27.26 to ✅ once the file exists and is consistently updated; but there is no scan step that checks whether the file is fresh; add a Scan 0 (pre-scan, runs before Scan 1) to `vibe-vision-auto.md` that checks: (a) if `docs/aide/loop-health.md` does not exist, append a `🔲 Urgent: SM has not written loop-health.md` comment to the report issue; (b) if the file exists but its git commit timestamp is >48h old, post a staleness warning to the report issue; this closes the feedback loop on 27.32 — the scan itself verifies that 27.32 was implemented, rather than waiting for a human to notice the file was never written
- 🔲 27.49 — Onboarding: `otherness-config.yaml` correctness validation — `/otherness.onboard` generates an `otherness-config.yaml` but there is no validation step that asserts the required fields are correctly populated before the first autonomous run; a new project with a wrong `report_issue` value will fail silently on every SM health comment with a `404 Not Found` from the GitHub API; a wrong `build_command` will cause every ENG phase to fail with a non-obvious error; add a `/otherness.validate-config` command (or a startup check in `standalone.md` §0) that runs before the first batch and asserts: `report_issue` is an open GitHub issue number; `build_command` exits 0 on the current codebase; `test_command` exits 0; `agents_path` resolves to a readable directory; post a clear error message for each failing check and block the batch; a new project should be validating its config within 5 minutes of running `/otherness.onboard`, not discovering config errors in session 10
- 🔲 27.50 — Honesty: vibe-vision-auto deduplication bug — this doc currently contains duplicate `🔲 Future` items with the same item numbers (27.40–27.45 appear twice in the System Loop Health section); the scan's Scan 3 insertion logic prepends new items before the Future section header but does not check whether the same item text was already appended by a previous run; the result is a corrupt doc that a human reviewer would flag as evidence the scan itself is unreliable; add a deduplication step to each vibe-vision-auto scan that checks for any item number appearing more than once in the same section and removes the duplicate before committing; the scan must be idempotent — running it twice must produce the same output, not doubled entries; this is a bug in the loop's own quality control, and it must be self-diagnosed and self-fixed before any other honesty improvements are credible
- 🔲 27.51 — Self-improvement: skill utilization tracking — the skills library has grown from 12 to 15 files but there is no evidence the new skills are being referenced by agents during sessions; `/otherness.learn` can write a skill file but there is no mechanism that forces COORD, ENG, or QA to consult the skills library before starting work; add a mandatory `## Skills consulted` section to the ENG phase work log that lists which skill files were read before starting the implementation; in SM §4b, count `skills_consulted` from the work log and add it as a metrics column; if `skills_consulted=0` for 3 consecutive batches despite `skills_count > 0`, the SM must add a `skills-ignored` defect item to the queue — a skills library that is never consulted provides zero improvement to agent quality regardless of its size
- 🔲 27.52 — Reliability: vibe-vision-auto scan output is never validated before commit — the scan scripts (Scan 1–5) write directly to `docs/design/*.md` files and commit without verifying that the resulting markdown is syntactically valid or that the Future section structure was preserved; a Python crash mid-write can produce a truncated or malformed doc that is silently committed; any subsequent scan that reads the truncated doc will silently skip it (the section regex finds no match) and the item queue degrades without detection; add a post-write validation step before the commit: parse each modified doc for balanced section headers (`## Present`, `## Future`, `## Zone`) and verify the file is non-empty and valid UTF-8; if validation fails, discard the write and post a `[SCAN ERROR]` comment to the report issue with the specific failure so a human can investigate; a scan that corrupts its own docs is worse than a scan that produces no output
- 🔲 27.53 — Visibility: skills library content quality is invisible — `skills_count=15` in the metrics table but there is no summary of what those 15 skills cover; a human reviewing the loop cannot tell whether the skills address the actual failure modes in the project or are generic boilerplate copied from the otherness default template; add a `docs/aide/skills-inventory.md` file (analogous to `loop-health.md`) that the SM updates monthly with: (1) a one-line summary of each skill file, (2) when it was last updated, (3) which session or bug prompted its creation, and (4) how many times it has been cited in `skills_consulted` logs; without this inventory the skills library is a black box — "15 skills" is a count, not an indicator of quality or coverage
- 🔲 27.54 — Reliability: vision-scan PRs accumulate unmerged — vibe-vision-auto runs as Step A and commits design-doc updates to a session branch, but if Step B is cancelled or ends without SM §4g merging the branch, the scan output sits in an open PR and never lands on `main`; design docs on `main` then show stale Future items even though the scan already promoted them in the unmerged PR; add a check to `vibe-vision-auto.md` post-scan that looks for open PRs with "vision(auto):" or "scans ran" in the title and posts a warning to the report issue if any are >24h unmerged; the scan must commit directly to `main` when running standalone (no Step B) to prevent perpetual doc drift
- 🔲 27.55 — Honesty: metrics batch number not updated after batch 51 — the last row in `docs/aide/metrics.md` is batch 51 (2026-04-20); subsequent sessions (PRs #602–#659, spanning 2026-04-21) have not added any rows; the SM must write a metrics row at the end of every session without exception; if the batch number is unavailable, use the PR count as a proxy; a metrics table last updated on 2026-04-20 when PRs are still merging on 2026-04-21 is evidence the SM is not reaching §4b
- 🔲 27.56 — Reliability: daily report issue rotation breaks SM health signal continuity — `REPORT_ISSUE` in `otherness-config.yaml` rotates daily (e.g. #439 → #637); a human looking at today's issue sees only today's batches; prior days' trends are buried in closed issues with no cross-link; add a permanent `[Loop Health Dashboard]` pinned issue that the SM always appends to regardless of which daily issue is active; the daily issue is fine for raw logs but must not be the sole health history record
- 🔲 27.57 — Self-improvement: loop-health `🔲` items in this doc sit unimplemented indefinitely — items 27.22–27.53 have been in the System Loop Health queue since 2026-04-20; none are ✅; the vibe scan's Scan 1 only promotes items when a merged PR title matches, but loop-health items require changes to `~/.otherness/agents/` files which are outside the repo and invisible to PR-title matching; add a Scan 1b to `vibe-vision-auto.md` for System Loop Health items that checks modification timestamps on referenced agent files (e.g. `sm.md`, `standalone.md`) against the item creation date; if the file was modified after the item was added, promote it to ✅; without this, the System Loop Health section becomes a permanent unverifiable graveyard
- 🔲 27.58 — Reliability: `docs/aide/loop-health.md` is missing on 2026-04-22 — items 27.26 and 27.32 specified this file in prior scans; it does not exist today despite multiple scan runs and 30+ PRs merged since the item was filed; the absence is concrete evidence that the SM's §4f is not being reached or the SM is not writing the file; this is the highest-priority loop health gap because all other health visibility improvements (27.26, 27.44, 27.48) depend on this file existing; the SM must write this file unconditionally at the end of every batch, even if it is a one-line stub; a scan that detects the file is missing must post an AMBER health comment to the report issue immediately — not merely queue a Future item — so the human knows the health dashboard is dark; until this file exists, the SM health signal is effectively unverified
- 🔲 27.59 — Visibility: vision-scan PR accumulation is confirmed active debt — as of 2026-04-22 there are open PRs from prior vibe-vision-auto runs that have not been merged to `main`; `docs/design/27-stage3-kro-tracking.md` on `main` does not reflect items 27.40–27.57 that were added in unmerged vision-scan PRs; item 27.54 describes the issue but does not provide a recovery path for the existing unmerged PRs; add an explicit SM recovery step to `standalone.md §4g`: at the start of each batch, before opening any new work, check for open PRs from prior vibe-vision-auto runs (title contains "vision(auto):") and close or squash-merge them to `main` immediately; stale vision-scan PRs are the most concrete form of doc rot — the future queue diverges from what `main` actually shows
- 🔲 27.60 — Honesty: GREEN health signal is structurally un-falsifiable — the SM posts GREEN when CI passes and a PR was merged, but CI for this project is `go vet + go test + bun typecheck + E2E`; an agent that merges only a `chore(config):` one-liner passes all CI checks and earns a GREEN; the health signal has no threshold for *substantive* shipping (design-doc-backed feature PRs with user-visible impact); a system that always returns GREEN regardless of actual output quality is not an honest health signal — it is a CI pass indicator wearing a health badge; add a `substantive_pr_count` to the SM health computation: a PR is substantive if its title starts with `feat:` AND it corresponds to an open GitHub issue AND that issue was linked to a design doc Future item; GREEN requires `substantive_pr_count >= 1`; AMBER means CI passed but 0 substantive PRs; RED means CI failed or 0 PRs total
- 🔲 27.61 — Reliability: vibe-vision-auto runs as Step A but Step B may never merge its output — the scheduled workflow is a 2-step process (Step A = vision scan, Step B = implementation run); if Step B is cancelled, times out, or the queue is empty and Step B exits early, the vision-scan branch PR is never merged; the COMMIT section of `vibe-vision-auto.md` explicitly says "commit to session branch, not main directly — SM §4g merges at batch end" but this presupposes Step B always runs to completion; add a recovery path: at the START of every Step A run (before scanning), check for open PRs with "vision(auto):" in the title that are >2h old; if found, merge them directly to main using `gh pr merge --squash --auto` before proceeding; this ensures doc updates from prior scans land on main even when Step B fails; without this, the vision scan is a write-only buffer that never feeds back into the codebase
- 🔲 27.62 — Self-improvement: pressure block in `otherness-scheduled.yml` is never auto-evolved by the scan — Scan 5 detects when the pressure block is ≥60% addressed and adds a Future item recommending a rewrite; but the rewrite itself requires a human to edit `otherness-scheduled.yml`; the scan adds a Future item but cannot update the workflow YAML because it is outside the `docs/` zone; the result is that the pressure block gets stale but never self-updates; item 27.43 proposes extracting the pressure block to `docs/aide/pressure-context.md` (a file in the DOCS zone that the scan CAN write); if 27.43 is implemented, Scan 5 should be extended to actually rewrite the pressure block in `pressure-context.md` rather than merely queuing a Future item — the scan should be the agent that raises the bar, not a human
- 🔲 27.63 — Onboarding: the scheduled workflow hard-codes the Step A prompt as a literal multi-line YAML string that must be manually updated per project — every project that uses otherness must copy-paste the entire vibe-vision-auto prompt block into its `otherness-scheduled.yml` and then customize the pressure section by hand; there is no mechanism to inherit the latest `vibe-vision-auto.md` changes (bug fixes, new scans) without editing the workflow file; add a `prompt: "read and follow ~/.otherness/agents/vibe-vision-auto.md"` form to the Step A invocation so the workflow is a 1-line pointer and the full scan logic lives in the agent file; when otherness ships a new scan (e.g. Scan 6 for vision.md auto-update), all projects pick it up automatically without a workflow PR; this is the same indirection that already works for Step B (`standalone.md`)
- 🔲 27.64 — Visibility: no machine-readable health endpoint for the loop — `docs/aide/loop-health.md` (27.26, 27.32, 27.58) is a human-readable markdown file; a dashboard or external monitoring tool cannot consume it without parsing markdown; add a `docs/aide/loop-health.json` companion file that the SM writes alongside `loop-health.md` with the same data in structured form: `{"health":"GREEN","last_feature_pr":{"number":686,"title":"...","merged_at":"..."},"open_blockers":3,"metrics_batch":52}`; a future external monitoring integration or GitHub Actions summary can consume this JSON without screen-scraping; the JSON format also makes it possible for the next session's COORD phase to read the prior session's health state and adjust its plan accordingly — the loop can finally "remember" what happened last time
- 🔲 27.65 — Reliability: SM phase rules in doc 27 are never proven to be implemented — items 27.22–27.64 are design doc `🔲 Future` items that describe changes to `~/.otherness/agents/phases/sm.md` and `~/.otherness/agents/standalone.md`; as of 2026-04-22 none of these rules appear in those files based on inspection; the design doc queue and the agent runtime are entirely decoupled; add an explicit implementation audit item: at the start of each vibe-vision-auto scan, read the SM phase file (`~/.otherness/agents/phases/sm.md`) and check for each System Loop Health item whether its described behavior (e.g. "if skills_delta=0 for 5 batches, invoke /otherness.learn") is present as an explicit code block or rule; items whose behavior is verifiably present in the phase file should be promoted to ✅; this closes the gap between "queued design intent" and "verified implementation" — without this the loop-health improvement queue is a wishlist, not a roadmap
- 🔲 27.66 — Reliability: every vibe-vision-auto run on this project adds more `🔲` items but the SM implementation that would close them is never updated — on 2026-04-22 there are 43+ System Loop Health items (27.22–27.64) none of which are ✅; each scan adds more items without first verifying whether prior items were implemented; this is infinite backlog inflation disguised as progress; add a hard rule to vibe-vision-auto: before adding any new System Loop Health items (27.xx), count the existing unimplemented items; if the count exceeds 20 and none have been implemented since the last scan, do NOT add new items — instead write a single summary `🔲 URGENT: implement the top 3 highest-priority loop-health items before this scan adds more`; queue inflation is itself a reliability failure — it signals the scan is producing output that the loop is constitutionally incapable of consuming
- 🔲 27.67 — Loop honesty: the metrics table has no row since batch 51 (2026-04-20) but 30+ PRs have merged — the most recent row is batch 51 from 2026-04-20; PRs #652–#688 merged on 2026-04-21 and 2026-04-22 with no corresponding metrics rows; the SM is either not running, not reaching §4b, or writing metrics to the wrong file/branch; item 27.55 documents this but has itself never been addressed; as a concrete forcing action: the vibe-vision-auto scan must now write a reconstruction row to `docs/aide/metrics.md` for the current date using `gh pr list --state merged` to count merged PRs since the last row date; this is not ideal (the scan is in the DOCS zone, not the SM role) but the alternative is an indefinitely stale metrics table — a permanently stale audit log is worse than an imperfect reconstruction
- 🔲 27.68 — Self-improvement: `loop-health.md` being missing is an active daily demonstration that the system is not self-improving — the file has been specified since 2026-04-20 (items 27.26, 27.32, 27.48, 27.58); it is 2026-04-22 and the file does not exist; this is not a design gap — it is an execution gap; the vibe-vision-auto scan is in the DOCS zone and CAN write this file; the scan should write a minimal stub on every run if the file is missing, containing at minimum: health status (derived from metrics last-row date vs today), last feature PR title and number, open blocker count from GitHub issues, and the instruction "this file is maintained by the SM — see docs/design/27-stage3-kro-tracking.md §27.32"; this converts the "write loop-health.md" item from a permanently-deferred SM task into a scan-maintained artifact that is always present, even if imperfect; a stub is infinitely better than nothing for a system that has had "write this file" in its queue for 3 days

---

## Zone 1 — Obligations

**O1 — kro upstream tracking is automated.** SM §4a must check for new kro releases
every batch and open issues automatically. Manual tracking is not acceptable.

**O2 — Every UI change for a new kro version ships with an E2E journey update.**
A kro API change with no journey update is a regression risk.

**O3 — v0.10.0 release requires all 27.2–27.6 items shipped.**
Do not cut v0.10.0 with open items from this list.

---

## Zone 2 — Implementer's judgment

- Accessibility checks: use `@axe-core/playwright` which is already in package.json
- Performance budget: use Lighthouse CI or simple `time curl` in E2E fixture
- Release: use `gh release create` with auto-generated notes

---

## Zone 3 — Scoped out

- Argo Rollouts integration (requires kro plugin ecosystem, not in v0.10 scope)
- GraphRevision diff viewer for >100 nodes (performance problem for later)
